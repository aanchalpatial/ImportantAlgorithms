//Naive approach takes 0(n) time & 0(n) space
//Two efficient approaches which takes 0(n) time & 0(1) space 

//1) Reverse Method
void rotateArr(int arr[], int d, int n){
    
   // Your code here
   reverse(arr, arr+d);
   reverse(arr+d, arr+n);
   reverse(arr, arr+n);
   
}

//2) Juggling Method
